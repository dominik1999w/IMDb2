package Controllers;

import java.net.URL;
import java.util.ResourceBundle;

public class movieScreenController extends Controller{
    movieScreenController(String name, Controller previousController){
        super(name,previousController);
    }

    @Override
    public void initialize(URL url, ResourceBundle resourceBundle) {

    }
}
